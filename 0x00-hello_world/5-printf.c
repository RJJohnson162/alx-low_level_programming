#include<stdio.h>

/**
 * main - prints the phrase with proper grammar.
 * but the outcome is a piece of art
 * Return: 0 if exited properly, not 0 otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
