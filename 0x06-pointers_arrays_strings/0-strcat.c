#include "main.h"

/**
 * _strcat - concatenates two strings: *dest & *src
 * @src: src string
 * @dest: dest string
 * Return: give the address of dest
 */

char *_strcat(char *dest, char *src)
{
    int a, b;

    a = b = 0;
    while (*(dest + a))
        a++;
    while ((*(dest + a) = *(srs + b)))
    {
        a++;
        b++;
    }
    return (dest);
}